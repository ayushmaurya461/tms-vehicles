import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tmslive.in',
  appName: 'Tms',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
